[package]
name = "velvt-shared-types"
version = "1.0.0"
edition = "2021"
publish = false

[features]
default = []
extensibility-proof = []

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1", features = ["serde", "v4"] }

[dev-dependencies]
