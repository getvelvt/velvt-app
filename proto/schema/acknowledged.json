{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$comment": "PRIVACY: This message type must never contain raw identifying user data.",
  "title": "Acknowledged",
  "description": "Confirms that the client and server protocol versions match.",
  "type": "object",
  "additionalProperties": false,
  "required": ["type"],
  "properties": {
    "type": { "type": "string", "enum": ["acknowledged"] }
  }
}
