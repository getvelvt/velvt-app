{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Acknowledged",
  "type": "object",
  "additionalProperties": false,
  "required": ["type", "payload"],
  "properties": {
    "type": { "const": "acknowledged" },
    "payload": {
      "$comment": "Rust's Acknowledged is a unit struct, which serde encodes as null, so null is what Rust sends. Until 2026-09-25 this schema said an empty object. Clients must accept null.",
      "type": "null"
    }
  }
}
