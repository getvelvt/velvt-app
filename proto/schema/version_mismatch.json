{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$comment": "PRIVACY: This message type must never contain raw identifying user data.",
  "title": "Version Mismatch",
  "description": "Reports incompatible server and client protocol versions.",
  "type": "object",
  "additionalProperties": false,
  "required": ["type", "expected", "got"],
  "properties": {
    "type": { "type": "string", "enum": ["version_mismatch"] },
    "expected": { "type": "integer", "minimum": 1 },
    "got": { "type": "integer", "minimum": 1 }
  }
}
