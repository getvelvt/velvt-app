{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": ["type", "payload"],
  "properties": {
    "type": { "const": "correct_event_classification" },
    "payload": {
      "type": "object",
      "required": ["event_id", "stable_id", "category"],
      "properties": {
        "event_id": { "type": "string", "format": "uuid" },
        "stable_id": { "type": "string" },
        "category": {
          "enum": ["FOCUS_WORK", "PASSIVE_CONSUMPTION", "SOCIAL_FEED", "COMMUNICATION", "TASK_MANAGEMENT", "REFERENCE", "SYSTEM", "UNLOGGED"]
        },
        "local_activity_name": {
          "type": "string",
          "minLength": 1,
          "maxLength": 48,
          "description": "Device-local user label. Never include it in cloud requests, telemetry, logs, or crash diagnostics."
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
