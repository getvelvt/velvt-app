{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$comment": "PRIVACY: This message type must never contain raw app names, window titles, bundle IDs, URLs, file paths, filenames, contact names, email addresses, or any other raw identifying user data.",
  "title": "Error Response",
  "description": "Typed IPC error envelope that either workspace may send.",
  "type": "object",
  "additionalProperties": false,
  "required": ["type", "code", "message"],
  "properties": {
    "type": {
      "type": "string",
      "enum": ["error_response"]
    },
    "code": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9]*(?:_[a-z0-9]+)*$"
    },
    "message": {
      "type": "string",
      "minLength": 1
    },
    "related_event_id": {
      "type": "string",
      "format": "uuid",
      "pattern": "^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-4[0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$"
    }
  }
}
